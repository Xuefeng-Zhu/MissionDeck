# Launch Review demo

This is a reproducible **fixture** workflow. It does not prove native provider writes or automation execution. Product name remains Mission Control.

1. Use Node 22+ and pnpm 10.32.1. Run `pnpm install --frozen-lockfile`, copy `.env.example` to a private `.env`, set `PROVIDER_MODE=fixture`, `MODEL_MODE=fixture`, and choose a fresh `DATA_DIR`. Ensure loopback 4318/5173 are free, then `pnpm dev` and `pnpm build`. Load `apps/extension/dist` unpacked in Chrome. Pair using the local pairing code (`pnpm pairing-code`); never place it in a webpage.
2. Create/confirm a mission for the launch review. Open these three local fixture pages: `http://127.0.0.1:4318/fixtures/launch-brief.html`, `/fixtures/launch-issues.html`, `/fixtures/launch-feedback.html`. All are labeled synthetic with a fixed September 12, 2026 source clock. Approval expiry always uses real time.
3. Open Browser context, choose these three tabs, and explicitly authorize their exact origin. Capture selected once. Inspect the table, literal formula-like string, truncated/partial state, and excluded private drafts. Review each relevant excerpt to save it as mission evidence. Unrelated picker metadata stays local.
4. Open Artifacts. Use the normalized review facts or saved evidence, select Docs/Sheets/Slides outputs, and prepare the plan. Review the source references, operations, targets and full content. Approve the exact displayed private-output plan. Inspect the fixture IDs and verified read-back. The task workflow remains in Plan with its own task approvals.
5. Prepare a second plan using the same facts. Existing verified fixture tracker and presentation are selected for update, preserving IDs. Review their before/after diff. Remove unnecessary outputs. Mail, Chat and Calendar preparation produces local proposals only; these are not saved native drafts, delivered messages or invitations. Timing is explicit UTC and availability is unknown.
6. In Browser context, separately opt into Live Assist and optionally automatic model sharing. With local extraction only, no context goes to a model. Click “Reveal new fixture requirement” on the brief; the changed source appears at the next bounded refresh. Inspect and intentionally save new evidence, then prepare a new artifact plan to see the updated content. The previous request remains frozen. Pause to clear temporary context and sharing; Manage Sites revokes grants.
7. For Make this repeatable, use an artifact plan containing only Docs/Sheets/Slides so every selected output is verified (local communication proposals are not verified outputs). Open Routines and choose Make this repeatable. This explicitly consents to retaining minimal execution metadata. Alternatively complete and verify the existing task mission first.
8. Click “Load three comparable fixture runs” to demonstrate the three-run heuristic. These synthetic histories are labeled; no native runs are created. Edit the routine, including its validated advanced fields, save a new version, and Preview. It reports zero production writes and concrete native blockers. Dismiss/suppress the pattern and verify it stays dismissed after reload.
9. Native enable and an actual run are **blocked** pending authorized node schemas and enforceable action scope. Neither the local preview nor comparable fixtures count as a native run. Live test-workspace authorization is required; no live writes were attempted during implementation.
10. Reload the extension. Mission evidence, artifact plan/results and routine versions persist on the local backend. Browser consent and collection remain paused. Native toolbar/context-menu side-panel opening, optional permission prompts, multiple-window focus and screenshots still require the manual check below.

## Manual browser checks

Click the toolbar on the intended ordinary page and confirm capture retains that source while the panel opens. Select safe page text and use the context menu; select private editable text and confirm refusal. Switch to an unauthorized site with the panel still open: no DOM access until an appropriate invocation/grant. Remove optional site permission during extraction: collection/sharing pauses. Close/reopen the panel and restart Chrome: Live Assist stays paused. Test two windows and navigation during capture. Explicit screenshot preview must show the intended visible tab, support local redaction/discard, and never send an image automatically. Image-to-model transmission is not implemented.

## Authorized live acceptance, later

Privately set provider keys and reviewed expected user/workspace IDs; keep model vendor selection explicit. Obtain authorization for a specific test workspace and exact test writes before changing to live execution. Inspect scoped objects and capability states. Docs native creation/replacement, mail draft and narrow slide paths require real read-back proof. Sheets literal encoding, rich slide elements, native communication/invitation proof and native automation nodes/policy boundaries remain access/schema gaps described in the capability matrix. The narrow send/invite adapters require a separate exact-content/source-sharing approval, then bounded audience/availability preflight. Do not treat a fixture pass, marketing documentation or an API acknowledgment as live acceptance.

The connected artifact panel can also prepare a separately categorized communication review when the live adapter supports a bounded destination. Such preparation/approval is separate from the fixture demonstration above; do not execute it without explicit test-workspace authorization. Native preflight permits only complete private/DM rosters and owned unpublished local calendars with complete availability coverage. Broader channels, group-shared calendars and missing access fail closed.
